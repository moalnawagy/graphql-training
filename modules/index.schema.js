const rootSchema = require("./root/root.schema")
const authSchema = require("./auth/auth.schema")
const userScheam = require("./user/user.schema")
const blogSchema = require("./Blog/blog.schema")
module.exports = {
    rootSchema,
    authSchema,
    userScheam,
    blogSchema
}